/**
 * Language chrome is hidden until locale routes are production-ready.
 * Interim: Traditional Chinese only — do not advertise 繁简EN日.
 */
export function LangSwitch(_props: {
  compact?: boolean;
  surface?: "navy" | "paper";
}) {
  return null;
}
